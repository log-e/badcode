
#include <string.h>
#include <stdlib.h>
#include <stdio.h>

int test_string(const char *src, size_t len)
{
    if(len > 6){
        if (src[0] == 'v') {
            if (src[1] == 'b') {
                // memcpy((void*)(src + 444), src, len);
            }
        }
    }
}